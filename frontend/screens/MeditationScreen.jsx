import * as React from "react";
import { View } from "react-native";
import styles from "../styles";

export default function MeditationScreen({ props }) {
    return (
        <View>

        </View>
    );
}